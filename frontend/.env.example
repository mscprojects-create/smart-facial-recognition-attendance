# Frontend talks to the Flask API through the Vite proxy (/api), so these are
# only needed if you ALSO want the browser to read Supabase directly.
VITE_SUPABASE_URL=https://YOUR-PROJECT-ref.supabase.co
VITE_SUPABASE_ANON_KEY=YOUR-anon-public-key
VITE_API_BASE=/api
